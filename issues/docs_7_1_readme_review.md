# README.md no completamente revisado

El archivo `README.md` principal podría necesitar una revisión integral para garantizar que los ejemplos reflejan la interfaz de línea de comandos actual.

Específicamente, hay que revisar que los ejemplos documenten los flags recientes como las opciones de compresión renovadas (`-z`, `-zz`, `-zzz`), `--timezone`, y la correcta eliminación del flag `--conflict keep`.
